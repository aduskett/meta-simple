[Unit]
Description=Setup directories on startup
After=data.mount factory.mount
Before=sshd.service

[Service]
ExecStart=/usr/bin/setup_directories
KillSignal=SIGINT
RemainAfterExit=yes
Type=oneshot

[Install]
WantedBy=multi-user.target
